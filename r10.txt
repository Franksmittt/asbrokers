================================================================================
  AS BROKERS CRM / PORTAL BUILD LOG — r10
  Phase 5: Security Patches, E.164 Phone, Supabase SSR Auth
  Date: June 2026
================================================================================

CONTEXT
-------
Implements r9 audit punch-list P0 fixes: correspondence traceability columns,
E.164 South African phone normalization, auto-provision inbound WhatsApp leads,
Supabase SSR middleware auth for /crm and /portal, and secured outbound
sendWhatsAppMessage Server Action.

SAFETY RULES OBSERVED
---------------------
- Did NOT touch app/studio/ or app/(content)/
- Used @supabase/ssr only (no auth-helpers-nextjs)
- await cookies() in lib/supabase/server.ts (Next.js 15 async dynamic APIs)

================================================================================
TASK 1 — SCHEMA TRACEABILITY & SUPABASE SERVER CLIENT
================================================================================

lib/db/schema.ts — correspondence updated:
- external_message_id  varchar(255) UNIQUE — Meta wamid dedup at DB layer
- staff_user_id        uuid → auth.users — outbound staff attribution
- Indexes: unique on external_message_id, index on staff_user_id

lib/supabase/server.ts (existing, verified compliant):
- createServerSupabaseClient() uses createServerClient from @supabase/ssr
- await cookies() before client init
- getAll() / setAll() only; setAll swallows errors in Server Components

================================================================================
TASK 2 — PHONE NORMALIZATION & UNMATCHED LEAD HANDLING
================================================================================

lib/whatsapp/phone.ts — sanitizePhone():
- Strips all non-digits (spaces, +, dashes)
- Local SA 0-prefix → prepend 27 (082… → 2782…)
- Already 27… → unchanged

lib/whatsapp/lead-phone.ts:
- leadPhoneMatches() SQL CASE mirrors sanitizePhone for JSONB phone field
- phoneFromRawPayload() uses sanitizePhone

trigger/whatsapp.ts:
- lead_not_found → auto INSERT crm_leads:
    pipeline_status: new
    service_category: unknown
    source_funnel: whatsapp_inbound
    raw_payload: { phone }  (E.164 normalized)
- INSERT correspondence with external_message_id = payload.messageId
- onConflictDoNothing on external_message_id (retry-safe)

================================================================================
TASK 3 — SECURE EDGE MIDDLEWARE (middleware.ts)
================================================================================

- Async middleware + createServerClient (@supabase/ssr) at edge
- getUser() verifies session token
- /crm and /portal without user → redirect /login?next=<path>
- /login with user → role redirect via app_metadata.role:
    admin | staff → /crm
    client        → /portal
- Supabase cookie refresh copied onto redirect responses
- Matcher excludes: _next/static, _next/image, favicon.ico, studio/, 
  api/webhooks/whatsapp, static file extensions
- Preserved: training-bot 403, URL normalize 301/410, X-Robots-Tag private routes

When NEXT_PUBLIC_SUPABASE_URL / ANON_KEY unset: auth gates skipped (mock login
still works via crm layout); SEO middleware unchanged.

================================================================================
TASK 4 — SECURE OUTBOUND WHATSAPP ACTION
================================================================================

app/actions/whatsapp.ts:
- createServerSupabaseClient() + getUser() before any API/DB work
- forbidden() (403) if Supabase unconfigured or user absent
- Graph API send unchanged (v19.0)
- Parses response messages[0].id → external_message_id when present
- correspondence insert includes staff_user_id: user.id

================================================================================
FILES TOUCHED (r10)
================================================================================

Modified:
- lib/db/schema.ts
- lib/whatsapp/phone.ts
- lib/whatsapp/lead-phone.ts
- trigger/whatsapp.ts
- app/actions/whatsapp.ts
- middleware.ts

Unchanged (verified):
- lib/supabase/server.ts
- app/studio/**, app/(content)/**

================================================================================
VERIFICATION
================================================================================

- npx tsc --noEmit — PASS

DEPLOY / FOLLOW-UP
------------------
- npm run db:push — apply correspondence column changes + unique index
- Set Supabase user app_metadata.role: Admin | Staff | Client on auth users
- Wire login page from mockLogin → Supabase magic link / email (Phase 5b)
- Replace crm layout mock-auth with Supabase session when ready
- Optional: restrict sendWhatsAppMessage to admin/staff roles only

================================================================================
END r10
================================================================================

================================================================================
  AS BROKERS WEBSITE - COMPLETE PROJECT OVERVIEW
================================================================================

Last updated: June 2026 (Phase 12 deployment readiness — SEO/AEO handbook, performance, CI/CD gates, unified JSON-LD, Playwright quality suite, homepage static optimisation)

--------------------------------------------------------------------------------
1. PROJECT OVERVIEW
--------------------------------------------------------------------------------

Project name:     as-brokers (AS Brokers website)
Repository:        https://github.com/Franksmittt/asbrokers.git
Framework:         Next.js 15 (App Router)
Language:          TypeScript
Styling:           Tailwind CSS
Key libraries:     Framer Motion, React Hook Form, Zod, clsx, tailwind-merge,
                   Recharts (calculator charts), Vercel AI SDK (ai, @ai-sdk/openai,
                   @ai-sdk/react), Drizzle ORM, pg, pgvector, server-only (HubSpot),
                   Resend (email); Trigger.dev v4 + Puppeteer (background PDF jobs).
                   Quiz: Zustand (state), nuqs (URL query state for shareable results).
                   Phase 3: next-sanity, sanity (v3), @portabletext/react; Studio deps:
                   is-hotkey, styled-components, hoist-non-react-statics, uuid (v11).
                   Phase 4: @next/third-parties (GA4); analytics only after cookie consent.
                   Quality / deploy: @playwright/test, @axe-core/playwright, @lhci/cli (Lighthouse CI).

Purpose:
  Marketing and lead-generation website for AS Brokers CC, an independent
  authorised financial services provider in South Africa. The site is
  positioned for high-net-worth individuals (HNWIs) and retirees: home
  page funnels to Run-Out Calculator and Everest Wealth (Code 1.8);
  solutions and calculators hubs prioritise "Private Wealth & Yield" and
  "Retirement Reality"; retirement, tax, estate, and premium calculators
  are framed as institutional fiduciary diagnostics with CTAs to Everest
  and contact. Standard insurance and estate/medical remain as "Wealth
  Protection & Fiduciary Defense." Interactive calculators and clear
  paths to contact and book consultations.

What this project can do:
  - Full marketing site: home (Reality Check hero, RunOutCalculator, EverestWealthBento, Code18Advantage, RiskArchitectureCarousel, FloatingChat), solutions hub (Phase 1/2 hierarchy), calculators hub (Phase 1/2), team, about, contact.
  - Contact form → HubSpot CRM (search-patch, cumulative lead score); optional PDF email via Trigger.dev + Resend.
  - Digital Wealth Assistant (chat) with deterministic tools (estate duty, 12.8% income, Amethyst) and optional RAG (pgvector).
  - Financial Health Quiz with shareable result URLs (nuqs) and routing to relevant calculators.
  - Insights publishing via Sanity CMS plus Client Blog Studio backed by Supabase/Postgres; draft mode, Visual Editing, FSP disclaimer blocks, and private uploaded blog images served through the existing media proxy.
  - Blog Studio supports loading existing posts, editing drafts/published posts, previewing current image slots, replacing/swapping images, and removing image assignments without deleting Supabase Storage objects.
  - POPIA cookie consent; GA4 and Hotjar only after "Accept All".
  - PWA manifest; full SEO/AEO stack (unified @graph JSON-LD per page, self-referencing canonicals, robots with training-bot blocks, sitemap, public/llms.txt, public/llms-full.txt, data-chunk-boundary sections, semantic comparison tables).
  - SEO audit suite: npm run seo:audit (static + runtime + internal link graph). Phase verification scripts (verify-phase1-edge through verify-phase12-master).
  - CI/CD: GitHub Actions — build, accessibility (axe-core), visual regression (Playwright Docker), Lighthouse CI (mobile ≥90), SEO/WRS tests, master-audit (npm run test:master).
  - Performance: homepage statically generated (~163 kB First Load JS); deferred below-fold JS; lightweight MarketingNavServer on /; Lighthouse gate on Linux CI.
  - Dynamic OG images (app/opengraph-image.tsx, app/api/og); image alt pipeline (data/image-metadata.json, lib/image-alt.ts, npm run alt:compile).
  - Edge middleware: URL normalisation (301), crawl traps (410), training-bot firewall (403), X-Robots-Tag on private routes.
  - Speculation rules (prerender /contact on moderate eagerness, client-injected after idle).
  - Run-out and product calculators (retirement, tax, estate, inflation, Everest Wealth, Amethyst).
  - Lead funnels: Retirement Readiness, Healthy Retirement Blueprint, Retirement Survival Blueprint, Business Risk Review, Legacy Readiness Checklist, Wealth Building Calculator, and related report flows.
  - CRM & Client Portal: mock-auth login (/login); role-based CRM (dashboard, Kanban, leads, clients, tasks, lead/client detail with correspondence, notes, reassign); Client Portal (wealth dashboard, My Advisor, documents with upload, messages with reply). See CRM_AND_PORTAL.txt.

Recent work done (Phases 1–12, June 2026):

  SEO / AEO / crawl infrastructure (Phases 1–5, 7, 10, 12):
  - middleware.ts: strip utm/fbclid/gclid, trailing-slash normalisation (301), crawl traps (410), training-bot 403, X-Robots-Tag noindex on /crm, /portal, /login, /studio, /internal.
  - app/robots.ts: disallow /api/; block GPTBot/ClaudeBot/CCBot; allow OAI-SearchBot, PerplexityBot, Claude-Web; sitemap + host from SITE_URL.
  - app/sitemap.ts: 50+ static marketing paths + Sanity articles + Client Blog Studio posts; revalidate 1h.
  - lib/seo.ts + lib/json-ld.ts: single @graph JSON-LD per page (Organization, FinancialService, LocalBusiness, WebPage, FAQ, Service, Product, Article, Breadcrumb, ImageObject); stable @id factory; XSS-safe escapeJsonLd (\u003c).
  - components/seo/PageJsonLd.tsx on indexable pages; FallbackPageJsonLdClient for client-only routes.
  - public/llms.txt + public/llms-full.txt (~1.3k BPE tokens, ceiling 128k); npm run b2a:compile.
  - lib/seo-metadata.ts helpers; generateMetadata on all dynamic routes; Twitter summary_large_image; /opengraph-image + /api/og dynamic OG.
  - data-chunk-boundary="true" on key content sections; semantic <table> comparison data (Everest products, premiums).
  - tests/seo.spec.ts: Googlebot UA, SSR title/keywords, payload <2MB, zero hydration errors, networkidle <5s, hero h1 Levenshtein ≤0.5%.
  - scripts/gsc-canonical-check.mjs; docs/DEPLOYMENT.md (GSC, pre-launch checklist).

  Accessibility & media (Phase 7):
  - data/image-metadata.json + scripts/generate-alt-text.mjs (npm run alt:compile); lib/image-alt.ts wired to BrandLogo, PageMediaStrip, ImagePlaceholder, Nav.
  - tests/accessibility.spec.ts: axe-core WCAG 2.1 AA on /, /contact, insights template; keyboard mobile nav; labelled contact form.

  Performance & Core Web Vitals (Phase 9):
  - Homepage static (○); First Load JS ~163 kB (down from ~389 kB).
  - HeroSection server-rendered (no Framer Motion on LCP path); BackgroundOrbs CSS gradients (no expensive blur filters).
  - MarketingNavServer + AppShellChrome: lightweight homepage nav/footer; full AppShellChrome dynamically loaded on other routes.
  - HomeDeferredBlocks / DeferUntilVisible: below-fold Recharts and Framer deferred until visible.
  - HomeInsightsTeaserStatic on homepage (no Sanity fetch on critical path); NuqsAdapter scoped to quiz layouts only.
  - System font stack (removed global next/font Inter); ContactEnquiryFormLazy on /contact.
  - SpeculationRulesClient: prerender /contact after idle.
  - lighthouserc.js + npm run test:lighthouse; scripts/verify-phase9-performance.ts.
  - tests/visual.spec.ts + Linux Docker CI baselines.

  CI/CD & master gate (Phase 12):
  - .github/workflows/ci.yml: build, accessibility, visual-regression (Playwright v1.49 Docker), lighthouse, seo-wrs, master-audit.
  - scripts/verify-phase12-master.ts + npm run test:master (lint, tsc, build, B2A, static SEO audit, a11y, SEO; Lighthouse on Linux CI).

  Prior feature work (retained):
  - Design system: Apple × Samsung hybrid (bg-[#0a0a0c] void, Bento grids, rounded-[2rem] squircle, cinematic-teal and blue/red accent glows, trust hallmarks FSP 17273 · Category 1.8 · 25+ Years).
  - Home: "Reality Check" hero; RunOutCalculator (#lab); EverestWealthBento; Code18Advantage; RiskArchitectureCarousel; FloatingChat.
  - Solutions hub, Calculators hub, Contact conversion page, Everest product pages, fiduciary calculator framing.
  - Blog Studio image editing; Sanity CMS + Visual Editing; HubSpot contact sync; Trigger.dev PDF + Resend.
  - CRM & Client Portal mocks; compliance wording pass (targeted/structured return profiles, not guaranteed returns).
  - Documentation: docs/calculator-overviews/, docs/DEPLOYMENT.md, docs/CUSTOMER_OVERVIEW.txt, CRM_AND_PORTAL.txt.

Data currency (2026):
  Income tax and Amethyst living-annuity tax estimates use SARS 2026/27
  brackets and rebates (Budget 2026). Estate duty, donations (R100k/R200k),
  and other calcs use current legislation; review periodically.

Design (current layout & styling):
  Fused "Apple Pro" and "Samsung Galaxy" design system for an ultra-luxury,
  futuristic private wealth terminal look.

  Colour & lighting:
    - Backgrounds: absolute black (--bg-primary: #000000, Tailwind bg-void) or Shark Grey (#1D1D1F, bg-shark).
    - Rim-light glassmorphism: .rim-light in globals.css (bg-white/5 backdrop-blur-2xl ring-1 ring-white/10 + inset edge highlight). Used for nav (when scrolled), cards, dropdowns, CTAs.
    - Nightography ambient: BackgroundOrbs — CSS gradient orbs (Cinematic Teal #008080, Gold/Orange #FF7F50); lightweight, no heavy blur filters on critical path.

  Geometry & layout:
    - Bento grid masonry for solutions and calculator hub cards.
    - Squircle: rounded-[2rem] or rounded-3xl (Tailwind) for organic curvature; no sharp corners on cards.
    - Generous padding and whitespace within cards (luxury signal).

  Typography:
    - System font stack (font-sans antialiased) for faster LCP; Inter retained in lib/fonts for OG image generation only.
    - Headlines: large, bold, tight tracking (tracking-[-0.03em]). Body: text-gray-400, slightly looser tracking. Trust signals use .trust-hallmark.

  Motion (Framer Motion):
    - Apple-style easing: cubic-bezier(0.25, 0.1, 0.25, 1) (ease-apple in Tailwind); duration 0.5–0.8s for page loads and layout changes.
    - CTA hovers: Samsung Blue (#0057B8) or Supernova Gold glow (shadow-cta-glow-blue / shadow-cta-glow-gold) with subtle scale-up (hover:scale-[1.03]).

  Trust signals:
    - FSP 17273, 25+ years, Code 1.8 rendered with .trust-hallmark for architectural precision (TrustBar, HomeStatsSection, Nav).

  Tokens (tailwind.config.ts & globals.css):
    - void, shark, cinematic-teal, gold-orange, samsung-blue, supernova-gold; transitionTimingFunction.apple; boxShadow rim-glow, cta-glow-blue, cta-glow-gold. Legacy .glass-card retained for compatibility.

--------------------------------------------------------------------------------
2. BUSINESS INFORMATION
--------------------------------------------------------------------------------

Legal name:        AS Brokers CC
Tagline:           "Protecting Your Legacy. Engineering Your Wealth."
FSP number:        FSP 17273
Regulation:        Independent Authorised Financial Services Provider
Category:          Category 1.8 (Securities and Instruments: Shares) for
                   advising on and distributing Everest Wealth products

Location:           Krugersdorp, West Rand, Gauteng, South Africa
Established:       Est. 1998 (25+ years experience)

Founders / team:
  - Albert Schuurman  – Co-founder; retirement income planning, certainty for clients
  - Johnny Farinha    – Co-founder; estate structuring, business continuity, risk planning

Services offered:
  - Retirement & investment planning (targeted income profiles, living annuities, RA)
  - Short-term insurance (personal: home, car, valuables; business: commercial, fleet)
  - Life insurance (personal: death, disability, income protection; business: key person, buy-and-sell)
  - Estate planning (wills, trusts, estate duty reduction)
  - Business structuring (buy-and-sell, key person, tax-efficient corporate protection)
  - Medical aid & gap cover structuring
  - Wellness & integration (high-income earners)
  - Everest Wealth products (Code 1.8 alternative / structured return-profile investments)

Contact:
  - WhatsApp: 067 242 9946
  - Contact form: /contact
  - No advice fees on investments; personal response (no call centre)

Key partners / product providers:
  - Everest Wealth Management (FSP 795) – structured product provider
  - Santam, Old Mutual, Bryte (recognised partners; referenced in trust bar)

--------------------------------------------------------------------------------
3. ALL PAGES (ROUTES & SUMMARIES)
--------------------------------------------------------------------------------

Quick index (all routes):
  /  /about  /calculators  /contact  /team  /regulatory-compliance  /how-we-work
  /solutions  /solutions/personal-insurance  /solutions/business-insurance
  /solutions/life-insurance  /solutions/business-life  /solutions/medical-aid
  /solutions/estate-planning
  /everest-wealth  /everest-wealth/about
  /everest-128-product  /immediate-higher-income-calculator  /everest-strategic-growth-145
  /everest-amethyst-living-annuity
  /lab  /retirement  /retirement-readiness  /income-in-retirement
  /cost-of-inflation-over-time  /income-tax-calculator  /estate-duty-calculator
  /annual-estate-reduction-strategy  /premium-increase-calculator  /wealth-building-calculator
  /healthy-retirement-blueprint  /retirement-survival-blueprint  /business-risk-review
  /legacy-conversations  /legacy-readiness-checklist
  /insights  /insights/[slug]  /insights/semigration-retirement
  /quiz  /chat  /privacy  /terms  /complaints  /conflict-of-interest  /manage-cookies
  /llms.txt  /llms-full.txt  /opengraph-image  /sitemap.xml  /robots.txt
  /studio/[[...tool]]  /studio/blog  /internal/pdf-report  /api/chat  /api/og  /api/draft-mode/enable
  /login  /crm/*  /portal/*  /embed/calculators/[id]

---

/ (Home)
  Static page (○). "Reality Check" hero; trust bar (FSP 17273 · Category 1.8 · 25+ Years); partner strip. RunOutCalculator (#lab). EverestWealthBento, Code18Advantage, RiskArchitectureCarousel. Below-fold blocks deferred (HomeDeferredBlocks, DeferUntilVisible): HomeStatsSection, HomeClientReviews, HomeInsightsTeaserStatic (no Sanity on critical path), HomeCtaStrip. MarketingNavServer (lightweight SSR nav with mobile menu); footer via lazy AppShellChrome. FloatingChat. First Load JS ~163 kB.

/about
  About AS Brokers CC: independent financial advisor Krugersdorp, West Rand, FSP 17273, Category 1.8. Links to Team and Contact. Metadata: independent financial advisor Krugersdorp.

/calculators
  "The Actuarial Reality Check" hero; jump nav (Assess Capital Lifespan, Calculate Everest Yields, Secondary Risk Tools). Phase 1: Capital Lifespan & High-Yield Solutions (retirement, income-in-retirement, inflation, Everest Product Quote — featured/glow cards). Phase 2: Secondary Risk Architecture (tax, estate, premium-increase — muted cards). Financial Education, Business Risk, CTA "Stop Guessing Your Capital Lifespan." FAQ schema. See docs/CALCULATORS_PAGE_OVERVIEW.txt.

/contact
  "Book a Private Actuarial Consultation"; Code 1.8 subhead; Apple-style trust tags (FSP 17273, 25+ Years, Zero Advice Fees); "Client Qualification Criteria" list; Squircle form card (border-blue-500/20 glow); submit "Initiate Wealth Engineering Request". "The Actuarial Review Process" 3 steps (Capital Assessment, Wealth Engineering Call, Implementation & Allocation). Form: fullName, phone, email, topics (Everest first), consent, honeypot. submitContactEnquiry → HubSpot + optional Trigger PDF. See docs/CONTACT_PAGE_OVERVIEW.txt.

/team
  Albert Schuurman and Johnny Farinha: roles, bios, FSP 17273. Metadata: independent financial advisors Krugersdorp.

/regulatory-compliance
  Code 1.8 FSP; FSCA Category 1.8 (Securities and Instruments: Shares); AS Brokers FSP 17273, Everest FSP 795. Metadata: Code 1.8 FSP license broker.

/solutions
  "Engineered Wealth & Risk Architecture" hero; sticky section nav (Private Wealth & Yield, Risk Architecture, Legacy Structuring, Health & Integration). Phase 1: Private Wealth & Yield — featured cards (Retirement Reality, Everest Wealth). Phase 2: Wealth Protection & Fiduciary Defense (Insurance, Estate, Medical — muted bg-[#101014] cards). CTA "Stop Guessing. Start Engineering." See docs/SOLUTIONS_PAGE_OVERVIEW.txt.

/solutions/personal-insurance
  Personal short-term insurance: home, car, valuables, asset protection.

/solutions/business-insurance
  Business short-term: commercial property, liability, business interruption, fleet.

/solutions/life-insurance
  Personal life: death cover, disability, income protection, severe illness.

/solutions/business-life
  Business life: buy-and-sell, key person, loan account, employee benefits.

/everest-wealth
  "The Alternative Yield Portfolio" hero; Code 1.8 Wealth Engineering Suite eyebrow; trust tags (FSP 17273, FSCA Category 1.8 (Shares), Zero Advice Fees) Apple-style. Phase 1: Immediate Cash Flow Architecture (targeted 12.8%, targeted 14.2% with "Max Day-1 Yield" tag). Phase 2: Pure Capital Compounding (targeted 14.5% card md:col-span-2). Phase 3: Retirement Liquidity Isolation (Amethyst "Section 14 Transfer Approved" tag). Copy uses targeted/structured return-profile language and explicit non-guarantee/risk disclosures. Cards hover:border-blue-500/40. Tertiary CTA "Read Fiduciary Briefing →" to /everest-wealth/about. FAQ schema. See docs/calculator-overviews/EVEREST_WEALTH_PAGE_OVERVIEW.txt.

/everest-wealth/about
  "Everest Wealth Explained": regulatory architecture, product suite, returns, risks, fees, tax. Everest FSP 795, AS Brokers FSP 17273, Category 1.8.

/everest-128-product
  "Yield Engineering: 12.8% Strategic Income." Code 1.8 Wealth Engineering eyebrow. Calculator in Bento (border-blue-500/20); monthly income and 5-year bonus in text-cinematic-teal. Fiduciary Compliance & Structure Bento (Regulated Efficiency 20% DWT card, Authority card). See docs/calculator-overviews/EVEREST_128_PAGE_OVERVIEW.txt.

/immediate-higher-income-calculator
  "Maximum Liquidity: 14.2% Onyx Income+." Dual Bento below calculator: The Mathematical Advantage, Ideal Capital Deployment (checkmarks). Squircle CTA "Request Official Onyx Term Sheet →" to /contact.

/everest-strategic-growth-145
  "Pure Compounding: 14.5% Strategic Growth." Calculator in Bento (border-blue-500/20); "Projected value at maturity" in text-cinematic-teal. The Tax Architecture card (20% DWT at maturity). CTA "Request 14.5% Term Sheet →" /contact.

/everest-amethyst-living-annuity
  "Insulated Retirement: The Amethyst Living Annuity." Phase 3 Retirement Liquidity Architecture eyebrow. 2-col Bento: The Yield Architecture (~10.2%, 9% bonus, SARS note), Strategic Deployment. Squircle CTA "Initiate Section 14 Pension Transfer Review →" /contact. FAQ schema.

/lab
  "The Lab": run-out calculator (portfolio, drawdown, lifespan; email for PDF) and links to Retirement Reality, Life cover, Estate liquidity.

/retirement
  "The Capital Deficit." Actuarial Reality Check eyebrow; red/amber risk orbs. 2-col Bento beneath hero: The Mathematical Drag, The Income Delusion (yield under 10% = principal drawdown). Calculator in Bento (border-red-500/20). Fix CTA "Erase the Deficit with Structured Yield" — primary "Calculate Amethyst Living Annuity (~10.2% Net) →" /everest-amethyst-living-annuity, secondary "Book Actuarial Consultation" /contact. Amethyst silo, quick links. FAQ schema.

/income-in-retirement
  "The Capital Depletion Test." Cinematic void + red/amber orbs. LifeOfCapitalCalculator in Bento (border-red-500/20); capital lifespan (years) in large text-cinematic-teal. Fix CTA card: "Does your trajectory show depletion?" — "Engineer a High-Yield Solution →" /everest-wealth, "Book Actuarial Review" /contact.

/cost-of-inflation-over-time
  "The Capital Erosion Test." Actuarial Reality Check eyebrow; red/amber orbs. FutureValueCalculator in Bento (border-red-500/20). 2-col Bento: The Mathematical Deficit (6–8% = zero real after inflation/tax), Outpacing the Erosion (12.8–14.5% structures). CTA "Stop Losing Purchasing Power" — "Engineer Inflation-Proof Yields →" /everest-wealth, "Book Actuarial Review" /contact.

/income-tax-calculator
  "The Income Tax Liability Engine." Phase 2 Risk Architecture · Tax Exposure. Calculator in Bento (border-white/10). Bento card "The Fiduciary Advantage: Tax Arbitrage" (marginal 45% vs 20% DWT, Everest preference shares). CTA "Restructure Your Tax Liability" — "Initiate Tax-Efficient Structuring →" /contact.

/estate-duty-calculator
  "Understand the cost of dying." Phase 2 Risk Architecture · Fiduciary Liability; red/amber orbs. Liquidity Stress Test Bento (20%/25% SARS, executor fees). The Hidden Problem (low-yield lock-up); Strategic Capital Migration (R100k/R200k). Squircle CTA "Engineer Your Estate Reduction Strategy →" /annual-estate-reduction-strategy.

/annual-estate-reduction-strategy
  "Strategic Capital Migration Plan." R100k/R200k exemption copy. Bento "How This Strategy Works in Practice" (trust + high-yield unlisted assets). Don't Wait (50→R3m, 60→R1m in text-cinematic-teal). CTA "Initiate Your Fiduciary Audit" — "Book Fiduciary Consultation" /contact.

/premium-increase-calculator
  "The Premium Liability Test." Phase 2 Uncapped Liability. PremiumComparisonCalculator in Bento (max-w-6xl, border-white/10). 3-col Bento: The Illusion (age-rated, compounding), The Trap (expiring guarantees), The Fiduciary Alternative (level/behaviour-linked). Squircle CTA "Stop Funding Structural Liabilities" — "Initiate Fiduciary Risk Audit →" /contact.

/insights
  Sanity-driven article list (GROQ insightsList). Links to /insights/[slug]. Empty state: coming soon + calculator/contact links.

/insights/[slug]
  Single article: cachedSanityFetch(articleBySlugQuery); title, date, excerpt, Portable Text with FSP disclaimer blocks. Locale from searchParams (default en).

/insights/semigration-retirement
  Static insight: semigration & retirement villages Western Cape; links to Retirement, Amethyst, Contact. Metadata: semigration, retirement villages western cape.

/quiz
  Financial Health Quiz: steps (concern → age → capital) → result. Zustand state; nuqs for shareable URL (?concern=, ?age=, ?capital=). Routing: Yields + capital > R100k → Everest 12.8%; Retirement 55+ → Amethyst; Estate Taxes → Annual Estate Reduction. CTA opens LeadFormContext.

/chat
  Digital Wealth Assistant: useChat; tools (estate duty, 12.8% income, Amethyst); RAG context when DATABASE_URL set. System prompt: R100k min, 120-day notice, 15% penalty, 20% DWT. Links to calculators, Contact.

/llms.txt
  Static file at public/llms.txt. AI discovery summary: AS Brokers CC, FSP 17273, Category 1.8, core services, sitemap/robots/llms-full URLs, crawler guidance. Verified by npm run seo:audit and verify-phase3-b2a.

/llms-full.txt
  Static file at public/llms-full.txt. Full AI briefing (~1.3k BPE tokens, ceiling 128k): entity, compliance, services, Everest risk language, medical/estate disclaimers, key URLs, citation guidance.

/opengraph-image
  Dynamic OG image (app/opengraph-image.tsx); 1200×630; default social preview for all pages.

/api/og
  Edge OG image API for per-page dynamic previews (query params).

/studio/[[...tool]]
  Embedded Sanity Studio v3; client-only (ssr: false). deskTool, presentationTool (draft mode /api/draft-mode/enable).

/studio/blog/workspace
  Client Blog Studio for AS Brokers-created posts. Private password-gated workspace; supports HTML paste, image slots, calculator/video slots, previews, draft save, publish, and existing-post editing. Image handling uses signed Supabase Storage uploads to the existing blog-images bucket and the /api/studio/media proxy. Current image slots are previewed and can be replaced/swapped or cleared without storage deletion, schema changes, or bucket/policy changes.

/internal/pdf-report
  Print-only page for Trigger.dev: searchParams portfolio, drawdown → Recharts AreaChart (no animation). Puppeteer captures to PDF; Resend emails attachment. No nav/footer in print.

/api/chat (POST)
  streamText, OpenAI gpt-4o-mini; tools calculateEstateDuty, calculateStrategicIncome128, calcAmethystAnnuity; RAG getRagContext(latestMessage, 5) injected into system prompt.

/api/draft-mode/enable (GET)
  next-sanity defineEnableDraftMode; enables draft for Visual Editing. SANITY_VIEWER_TOKEN.

/login
  CRM & Client Portal mock login. Choose role (Admin, Staff, Client); Staff pick advisor from dropdown. Sets cookies (mock-crm-role, mock-crm-user, mock-crm-staff-id); redirects to /crm (admin/staff) or /portal (client). See CRM_AND_PORTAL.txt.

/crm
  CRM dashboard: pipeline stats (New→Won/Lost) and recent leads; role-filtered (admin = all, staff = assigned). Links to Kanban and Leads.

/crm/kanban
  Pipeline Kanban: columns New, Contacted, Qualified, Proposal, Won, Lost; drag-drop cards (mock, resets on refresh). Role-filtered. Cards link to lead detail.

/crm/leads
  Leads table: Name, Contact, Service, Intent, Status, Assigned, Fit/Eng. Role-filtered. Name links to /crm/leads/[id].

/crm/leads/[id]
  Lead detail: contact, intent, status, service, reassign (admin only), correspondence thread, notes, tasks.

/crm/clients
  Clients table (converted leads); role-filtered. Name links to /crm/clients/[id].

/crm/clients/[id]
  Client detail: contact, services, advisor, WhatsApp; same correspondence, notes, tasks as CRM side.

/crm/tasks
  Tasks list: To do and Completed; linked to lead/client; role-filtered by assignee.

/portal
  Client Portal dashboard: My Advisor (name, WhatsApp, Call), portfolio value, monthly income, holdings list, Recharts (income + drawdown). Mock data.

/portal/documents
  Document vault: mock list + DocumentUpload (mock file upload). Backend will use Supabase Storage + audit.

/portal/messages
  Unified message thread (portal/email/WhatsApp) + MessageReply (mock send). Same thread as CRM client/lead correspondence.

Route structure (App Router):
  Root layout: app/layout.tsx (system font-sans, bg-void); FallbackPageJsonLdClient; Providers (ConsentProvider only — no global MotionConfig/NuqsAdapter); AppShell; SpeculationRulesClient.
  Homepage (/): MarketingNavServer + main + lazy AppShellChrome (footer, FloatingWhatsApp, etc.). Other routes: full AppShellChrome (Nav + Footer + lead form context).
  Route group (content): app/(content)/layout.tsx wraps public routes; Visual Editing in app/(content)/insights/layout.tsx when draft mode. Quiz/chat layouts scope NuqsAdapter locally.
  middleware.ts: matcher excludes static assets (_next, images, fonts, favicon) so case-sensitive /images/* paths are never rewritten.
  Studio, CRM, portal, API, embed routes outside lightweight homepage chrome.

--------------------------------------------------------------------------------
4. SITE STRUCTURE & NAVIGATION
--------------------------------------------------------------------------------

Header (global):
  - Homepage (/): MarketingNavServer — SSR lightweight nav, logo (BrandLogo /images/logo.jpg), direct links (Solutions, Calculators, Team, Contact), mobile hamburger with keyboard support.
  - All other routes: components/Nav.tsx — full rim-light nav, CRM & Client Portal link, frosted glass on scroll.
  - On /crm, /portal, /login: minimal bar (logo, Back to site, Switch account).
  - Skip link: "Skip to main content" (focus-only).
  - No dropdown menus: Solutions and Calculators are single links to hub pages.

Footer:
  - Compliance strip: FAIS compliant, FSCA regulated, POPIA compliant, FSP 17273
  - Brand (logo, tagline, FSP, WhatsApp button)
  - Quick links: Home, About, Solutions, Chat (Wealth Assistant), Calculators,
    Insights, Financial Health Quiz, Contact, Team, Regulatory & Compliance
  - Services: Retirement & Everest, Insurance & Risk, Estate, Understanding Everest
  - Trust badge: Authorised Financial Services Provider | FSP 17273
  - Legal: Privacy Policy, Terms of Use, Complaints Procedure, FAIS & POPIA
  - Copyright AS Brokers CC

PWA (installability):
  - Manifest: /public/manifest.json
  - Note: No service worker is implemented yet. Offline calculator support would
    require adding a service worker (Phase 5).

--------------------------------------------------------------------------------
5. KEY COMPONENTS (NON-PAGE)
--------------------------------------------------------------------------------

AppShell          – Routes / to MarketingNavServer + lazy AppShellChrome; other routes to full chrome
AppShellChrome    – Footer, FloatingWhatsApp, QuickActionBar, LeadFormContext; dynamically imported
MarketingNavServer – SSR homepage nav (desktop + mobile menu); a11y-tested keyboard toggle
Nav               – Full fixed header for non-home routes; rim-light on scroll; BrandLogo
BrandLogo         – next/image /images/logo.jpg with lib/image-alt getAlt()
Footer            – Multi-column footer; FOOTER_EXPLORE / FOOTER_LEGAL from lib/site-navigation; newsletter Server Action
HeroSection       – Server-rendered home hero (no Framer Motion on LCP path)
HeroChatTerminal  – AI chat terminal (FloatingChat panel and /chat)
BackgroundOrbs    – CSS gradient orbs (lightweight; no heavy blur on critical path)
EverestWealthBento, Code18Advantage, RiskArchitectureCarousel – Home sections
HomeDeferredBlocks / DeferUntilVisible – Below-fold lazy loading for charts and motion
HomeInsightsTeaserStatic – Static insights teaser on homepage (no Sanity fetch on critical path)
FloatingChat      – Fixed bottom-right FAB; HeroChatTerminal panel
RunOutCalculator  – Portfolio + drawdown → capital lifespan; POPIA gate for PDF via Trigger.dev
ContactEnquiryForm – Server Action submitContactEnquiry; HubSpot sync; honeypot; Zod validation
ContactEnquiryFormLazy – dynamic import on /contact for smaller initial bundle
ContactPageView   – Contact page layout wrapper
PageJsonLd        – Server component: single @graph JSON-LD per page via lib/seo.ts buildPageGraph()
FallbackPageJsonLdClient – Client fallback graph for routes without server PageJsonLd
SpeculationRulesClient – Injects speculationrules JSON (prerender /contact, moderate eagerness)
RelatedContent    – Internal linking component for SEO silos
EverestProductComparisonTable – Semantic table for Everest product comparison (AEO)
LeadFormContext   – Global lead form modal
EverestLiquidityWarning – 120-day notice, 15% early exit penalty (voluntary products)
Skip link         – Global accessibility skip link to #main-content

Analytics & consent (Phase 4):
  ConsentProvider        – components/analytics/ConsentProvider.tsx; context (consent, setConsent);
                            reads/writes localStorage (asbrokers-cookie-consent); renders CookieConsent + ConditionalAnalytics
  CookieConsent           – components/ui/CookieConsent.tsx; fixed bottom banner (POPIA copy); Accept All / Essential Only
  ConditionalAnalytics    – components/analytics/ConditionalAnalytics.tsx; mounts GA4 + Hotjar only when consent === "all"
  HotjarAnalytics         – components/analytics/HotjarAnalytics.tsx; next/script lazyOnload Hotjar snippet

Sanity / Portable Text (Phase 3):
  sanity/lib/client.ts     – createClient (next-sanity); stega when studio URL set
  sanity/lib/live.ts       – getClient() (draft-aware token), sanityFetch()
  sanity/lib/fetch.ts      – cachedSanityFetch (React cache) for shared article request
  sanity/lib/queries.ts    – insightsListQuery, articleBySlugQuery (body with fspDisclosure→fsp)
  sanity/config.ts         – deskTool, presentationTool; schema from schemaTypes
  sanity/schemaTypes/     – seoMetadata, article (slug unique per locale), fspEntity, fspDisclosure
  components/portable-text/ArticlePortableText.tsx – PortableText; types.fspDisclosure → FspDisclosureBlock
  components/portable-text/FspDisclosureBlock.tsx  – Styled disclaimer box (fsp.name, fspNumber, disclaimerText)

Financial Health Quiz (Phase 2):
  store/useQuizStore.ts   – Zustand: ageBracket, primaryConcern, availableCapital;
                            hasCapitalOver100k() for Everest 12.8% rule
  app/(content)/quiz/page.tsx – Multi-step client component; nuqs (concern, age, capital)
                            for shareable result URL; AnimatePresence + useReducedMotion;
                            QuizResults applies routing rules, CTA opens LeadFormContext.
                            Wrapped in Suspense for useQueryState/useSearchParams.
  app/(content)/quiz/layout.tsx – NuqsAdapter scoped to quiz routes only

Digital Wealth Assistant (chat):
  app/api/chat/route.ts   – POST handler; streamText, system prompt, three tools
  app/api/chat/schemas.ts – Zod: calculateEstateDuty, calculateStrategicIncome128,
                            calcAmethystAnnuity (R100k min, 2.5–17.5% drawdown)
  app/api/chat/tools.ts   – Deterministic: estate duty, 12.8% income, Amethyst tax
  app/(content)/chat/page.tsx – useChat, DefaultChatTransport, tool result cards

Calculator components (one per calculator page):
  RetirementRealityCalculator (incl. scenario + Recharts bar), LifeOfCapitalCalculator,
  CostOfInflationCalculator, IncomeTaxCalculator, EstateDutyCalculator,
  AnnualEstateReductionCalculator, PremiumIncreaseCalculator,
  Everest128Calculator, ImmediateHigherIncomeCalculator,
  EverestStrategicGrowth145Calculator, AmethystLivingAnnuityCalculator

CRM & Client Portal (see CRM_AND_PORTAL.txt):
  CrmSidebar         – Desktop: nav (Dashboard, Kanban, Leads, Clients, Tasks), role/name, Log out, Back to site. Mobile: top bar with same links.
  LeadsKanban        – Client component: columns by lead status; drag-drop cards; role-filtered initialLeads.
  PortalNav          – Fixed top nav: logo, Dashboard/Documents/Messages, user, Log out, Site.
  PortalDashboardCharts – Recharts: cumulative income area chart, annuity drawdown area chart (mock data).
  PortalMyAdvisor    – Card: advisor name, role, WhatsApp button, Call link.
  DocumentUpload     – Mock file input; appends “uploaded” item to list (no real storage).
  MessageReply       – Textarea + Send (mock); shows “Sent (mock)”.
  Mock data: lib/mock-auth.ts (session cookies), lib/mock-crm.ts (leads, clients, staff, correspondence, tasks, notes), lib/mock-portal.ts (holdings, documents, messages, advisor, chart data).

--------------------------------------------------------------------------------
6. METADATA & TECHNICAL SEO
--------------------------------------------------------------------------------

Root layout:
  Title template:  %s | AS Brokers CC
  Default title:   AS Brokers CC | Independent Financial Advisor Krugersdorp
  metadataBase:    getSiteOrigin() (NEXT_PUBLIC_SITE_URL)
  Canonical:       self-referencing per route via lib/seo-metadata.ts / page metadata
  OpenGraph:       type website, locale en_ZA, siteName AS Brokers CC, /opengraph-image 1200×630
  Twitter:         summary_large_image, /opengraph-image
  Google verification meta tag present.

Page-level metadata (keyword targeting per SEO plan):
  /about                    – independent financial advisor Krugersdorp
  /contact (layout)         – independent financial advisor Krugersdorp
  /regulatory-compliance    – Code 1.8 FSP license broker
  /everest-wealth           – alternative private equity South Africa, Everest Wealth brokers
  /retirement               – semigration, retirement villages Western Cape
  /insights/semigration-retirement – semigration, retirement villages Western Cape
  /team                     – independent financial advisors Krugersdorp

Structured data (lib/seo.ts, lib/json-ld.ts):
  - Single @graph per page template via PageJsonLd + buildPageGraph().
  - Stable @id factory (createSeoIds): organization, website, financialService, localBusiness,
    person nodes (Albert, Johnny), webPage, faqPage, article, service, product, breadcrumb, primaryImage.
  - Organization + FinancialService + LocalBusiness on every indexable page; FAQ/Service/Product/Article
    nodes when applicable. Zero orphan @id references (all cross-refs use @id).
  - escapeJsonLd in lib/json-ld.ts: XSS-safe (\u003c, \u003e, \u0026).
  - FallbackPageJsonLdClient for client-only routes.

Semantic silos & internal linking:
  - RelatedContent component; footer discoverability links (FOOTER_EXPLORE, FOOTER_LEGAL).
  - Retirement → Amethyst; estate duty → annual estate reduction; calculators hub silos.
  - Internal link graph audit: npm run seo:audit:links (0 orphan indexable pages target).

Sitemap / GSC:
  - app/sitemap.ts: STATIC_PATHS (50+ routes) + Sanity articles + Client Blog Studio posts.
  - Production URL: https://www.asbrokers.co.za/sitemap.xml
  - Google Search Console verification meta in root layout.
  - Post-deploy: npm run gsc:canonical (scripts/gsc-canonical-check.mjs).
  - Full setup: docs/DEPLOYMENT.md

SEO/AEO audit & verification scripts:
  npm run seo:audit          – full suite (static + runtime + links)
  npm run seo:audit:static   – App Router structural SEO/AEO checks
  npm run seo:audit:runtime  – robots, sitemap, llms, canonical, noindex, JSON-LD, bot policy
  npm run seo:audit:links    – sitemap-driven internal link graph
  npm run b2a:compile        – regenerate public/llms.txt + llms-full.txt
  npm run alt:compile        – regenerate image alt metadata
  npm run gsc:canonical      – GSC URL Inspection canonical parity (post-deploy)
  npx tsx scripts/verify-phase3-b2a.ts  – B2A token ceiling + chunk boundaries
  npx tsx scripts/verify-phase12-master.ts – master pre-deploy gate (npm run test:master)
  Generated reports gitignored: asbrokers_*_findings.txt, test-results/, lhci-reports/

PWA:  public/manifest.json (name, theme, start_url). Root layout references manifest.

--------------------------------------------------------------------------------
7. CURSOR AI & PROJECT MEMORY (Rolls-Royce Blueprint)
--------------------------------------------------------------------------------

Memory bank:
  - BACKEND.md         – Backend setup checklist; env vars; Supabase, HubSpot, Trigger.dev, Resend, Sanity, OpenAI, RAG
  - docs/DEPLOYMENT.md – CI gates, GSC setup, pre-launch checklist, master-audit, post-deploy monitoring
  - CRM_AND_PORTAL.txt – CRM & Client Portal mock vs planned backend
  - ROLLS_ROYCE_CRM_VISION.txt – Vision for bespoke CRM (client portal, staff, owner dashboards)
  - .cursor/rules/crm-rolls-royce.mdc – Cursor rule for CRM/portal work
  - agents.md          – Tech stack, business context, architecture, Everest constraints
  - app/(content)/calculators/CONTEXT.md – Formula and tax context for all calculators
  - docs/CUSTOMER_OVERVIEW.txt – Customer-facing project and feature overview
  - docs/SOLUTIONS_PAGE_OVERVIEW.txt, CALCULATORS_PAGE_OVERVIEW.txt, CONTACT_PAGE_OVERVIEW.txt
  - docs/calculator-overviews/ – Per-calculator page overviews; INDEX.txt

Cursor rules:
  - .cursor/rules/asbrokers.mdc – RSC default, Server Actions for mutations,
    Zod at boundaries, Tailwind glassmorphism, prefers-reduced-motion, Everest
    R100k min and liquidity copy. alwaysApply: true.

Server Actions:
  - app/actions/contact.ts – submitContactEnquiry(prevState, formData) for useActionState.
    contactFormSchema (Zod); flatten().fieldErrors for UI; honeypot; syncContactToHubSpot
    (lib/hubspot.service). After HubSpot success, triggers PDF job via POST to Trigger.dev
    API (generate-financial-pdf) so client gets immediate success; PDF generated async.
    Returns ContactActionState (success, message, fieldErrors).

Validations:
  - lib/validations/schema.ts – contactFormSchema (main form), financialCalculatorSchema
    (contact + optional capitalAmount for calculator lead capture), ContactActionState.
  - lib/validations/investmentSchema.ts – investmentLeadSchema with R100,000 minimum
    (EVEREST_MIN_INVESTMENT) for Everest voluntary products; for use on lead forms.

HubSpot CRM (Phase 4):
  - lib/hubspot.service.ts – "server-only"; getContactByEmail (GET by idProperty=email,
    returns id + platform_lead_score); syncContactToHubSpot: search-patch (PATCH if
    exists, POST if 404). Cumulative lead score: read existing + add (+20 if
    capitalAmount > R1M, +15 if topic short_business or trust). Properties:
    financial_inquiry_topic (string), financial_capital_input (when capital present),
    platform_lead_score (string). Env: HUBSPOT_ACCESS_TOKEN or HUBSPOT_PRIVATE_APP_TOKEN.

--------------------------------------------------------------------------------
8. SEO IMPLEMENTATION SOURCE
--------------------------------------------------------------------------------

Implementation follows: Next.js SEO & Schema Implementation Plan.txt
(Architectural blueprint: global Organization/FinancialService schemas, FAQPage
per product/calculator, metadata keyword strategy, semantic siloing, RSC-first,
prefers-reduced-motion, XSS-safe JSON-LD.)

--------------------------------------------------------------------------------
9. GENERATIVE UI CHATBOT (DIGITAL WEALTH ASSISTANT)
--------------------------------------------------------------------------------

Source: Generative UI Chatbot Implementation Plan.txt

Architecture:
  - LLM as router only; all financial math in deterministic TypeScript tools
  - Zod validation on tool inputs (R100k min, 2.5–17.5% drawdown, non-negative)
  - Baseline system prompt hardcodes FSCA constraints (R100k, 120-day notice, 15% penalty, 20% DWT)
  - RAG pipeline: before each streamText call, the latest user message is embedded and
    used for cosine-similarity search; retrieved chunks are injected into the
    "Interceptor System Prompt" below the baseline (baseline unchanged)

Tools:
  - calculateEstateDuty: gross estate, liabilities, spousal inheritance, charity;
    executor 4.025%, abatement R3.5m, duty 20%/25%
  - calculateStrategicIncome128: capital ≥ R100k; 12.8% gross, 20% DWT, 10% bonus
  - calcAmethystAnnuity: capital ≥ R100k, drawdown 2.5–17.5%; SARS 2026/27 65+ tax

Requires: OPENAI_API_KEY in environment for /api/chat. DATABASE_URL optional (see RAG).

--------------------------------------------------------------------------------
10. RAG PIPELINE (RETRIEVAL-AUGMENTED GENERATION)
--------------------------------------------------------------------------------

Purpose: Ground the chat assistant in Everest Wealth and FAIS/SARS documentation
via pgvector similarity search. Baseline constraints remain in code; RAG adds
retrieved context only.

Database (Drizzle ORM + PostgreSQL + pgvector):
  - lib/db/schema.ts:
    - resources: id, content (text), source (varchar 255), metadata (jsonb), created_at
    - embeddings: id, resource_id (FK → resources.id ON DELETE CASCADE), embedding (vector(1536)), created_at
    - HNSW index on embeddings.embedding with vector_cosine_ops for cosine similarity
  - Extension: CREATE EXTENSION vector; (run manually or via custom migration)
  - drizzle.config.ts: schema ./lib/db/schema.ts, out ./drizzle, dialect postgresql, dbCredentials.url from DATABASE_URL

Retrieval:
  - lib/db/rag.ts:
    - getRagContext(query, limit?) → Promise<string>
    - Embeds query via AI SDK embed + openai.embedding('text-embedding-3-small')
    - If DATABASE_URL missing: returns "" (no DB connection)
    - Otherwise: Drizzle select from embeddings join resources, orderBy sql`embedding <=> $vector::vector`, limit 5
    - Returns concatenated "[Source: ...]\ncontent" blocks or "" on error
  - lib/db/index.ts: getDb() returns Drizzle client or null; lazy pool from DATABASE_URL

Chat route (app/api/chat/route.ts):
  - getLatestUserMessageText(messages) → last user message text from parts/content
  - systemPrompt = BASELINE_SYSTEM_PROMPT + "\n\n[Retrieved context]:\n" + (await getRagContext(latestText, 5)) or "[No relevant context retrieved.]"
  - streamText({ system: systemPrompt, ... }) — baseline constraints untouched

Environment:
  - OPENAI_API_KEY: required for chat (and for RAG embedding)
  - DATABASE_URL: optional; if set, RAG retrieval runs; if unset, chat still works with empty retrieved context

Document ingestion:
  - scripts/ingest-rag.ts: chunks .txt/.md from scripts/rag-documents (or custom dir), embeds with text-embedding-3-small, inserts into resources + embeddings. Run: npm run rag:ingest (needs OPENAI_API_KEY, DATABASE_URL). Plan: docs/RAG_IMPLEMENTATION_PLAN.md.

--------------------------------------------------------------------------------
11. PHASE 2: PDF GENERATION & EMAIL (TRIGGER.DEV + RESEND)
--------------------------------------------------------------------------------

Asynchronous print route + background job so serverless timeouts and Recharts DOM
requirements are avoided; PDFs are generated in Trigger.dev workers and emailed via Resend.

Print route (app/internal/pdf-report):
  - page.tsx: Client component; useSearchParams() for portfolio, drawdown. Run-out
    chart data; Recharts AreaChart with isAnimationActive={false} (required for
    Puppeteer capture). Wrapper class pdf-content for print visibility.
  - print.css: @media print { body * hidden; .pdf-content visible }; @page { size: A4; margin: 0 }.
  - layout: robots noindex,nofollow. No navigation/footer in DOM; print CSS isolates content.

Trigger.dev v4 (trigger/generatePdf.ts, trigger.config.ts):
  - Task id: generate-financial-pdf. Payload: email, fullName?, portfolio?, drawdown?.
  - trigger.config.ts: maxDuration 60; build.extensions [puppeteer()], build.external ["jose", "puppeteer"]; puppeteer as devDependency for bundle resolve.
  - Puppeteer: launch headless, goto APP_URL/internal/pdf-report?portfolio=...&drawdown=..., page.pdf(A4, printBackground) → buffer.
  - Resend: send to payload.email, subject "Your AS Brokers Wealth Projection", attachment PDF.
  - App triggers via HTTP: POST api.trigger.dev/.../generate-financial-pdf/trigger with TRIGGER_SECRET_KEY.

Env:
  - Next: TRIGGER_SECRET_KEY (to enqueue job).
  - Trigger worker: APP_URL or NEXT_PUBLIC_APP_URL, RESEND_API_KEY, RESEND_FROM (optional),
    PUPPETEER_EXECUTABLE_PATH (optional per Trigger docs).

--------------------------------------------------------------------------------
12. PHASE 3: SANITY V3 HEADLESS CMS
--------------------------------------------------------------------------------

Purpose: Replace /insights placeholder with Sanity-driven articles, draft mode,
and Visual Editing; FAIS-oriented FSP disclaimers as inline blocks in article body.

Architecture:
  - Route group (content): app/(content)/layout.tsx wraps front-facing routes with
    conditional <VisualEditing /> when draftMode().isEnabled. Studio at /studio
    is outside (content) to avoid reload/overlay conflicts.
  - Document model: Single article document with locale field ('en'|'af'); slug
    validated unique per locale (custom rule in article schema). No document-i18n plugin.
  - FSP disclaimer: Resolved in GROQ (articleBySlug expands body so fspDisclosure
    blocks include "fsp": fspEntity->). Frontend serializer only renders resolved data.

Schemas (sanity/schemaTypes/):
  - seoMetadata: metaTitle (max 65), metaDescription (max 160), canonicalUrl,
    openGraphImage, noIndex.
  - article: title, slug, locale, publishedAt, excerpt, body (block + fspDisclosure), seo.
  - fspEntity: name, fspNumber, disclaimerText.
  - fspDisclosure: block type; reference to fspEntity (resolved in GROQ).

Data fetching:
  - getClient() in live.ts: returns client with viewer token + previewDrafts when
    draftMode().isEnabled; otherwise default client.
  - cachedSanityFetch (fetch.ts): React cache(sanityFetch) so generateMetadata
    and page share one request per article.
  - Queries: insightsList (ordered by publishedAt); articleBySlug(slug, locale)
    with body[] expanded for fspDisclosure → fsp.

Studio:
  - app/studio/[[...tool]]/page.tsx: dynamic import of NextStudio + config (ssr: false)
    to avoid server-bundle issues. Config: deskTool(), presentationTool(previewUrl
    with previewMode.enable: /api/draft-mode/enable).

Build / env:
  - next.config.ts: webpack alias/fallback for hoist-non-react-statics (path to
    dist/hoist-non-react-statics.min.js) so @sentry/react (pulled in by sanity) resolves.
  - Sanity client uses projectId || "placeholder" when NEXT_PUBLIC_SANITY_PROJECT_ID
    unset so build (e.g. draft-mode route) does not throw.
  - Env: NEXT_PUBLIC_SANITY_PROJECT_ID, NEXT_PUBLIC_SANITY_DATASET,
    NEXT_PUBLIC_SANITY_STUDIO_URL, SANITY_VIEWER_TOKEN. See .env.example.

--------------------------------------------------------------------------------
13. CRM & CLIENT PORTAL
--------------------------------------------------------------------------------

Purpose: Authenticated CRM for staff/owners and Client Portal for clients; currently
mock data and mock auth (cookie-based role/staff). Built for backend swap (Supabase,
RLS, Magic Links, HubSpot sync).

Entry: Nav "CRM & Client Portal" → /login. Choose Admin / Staff / Client; Staff
also choose team member. Redirect to /crm or /portal.

CRM (admin/staff):
  - Dashboard: pipeline counts + recent leads (role-filtered).
  - Kanban: drag-drop pipeline (New→Won/Lost); role-filtered.
  - Leads: table + detail (contact, correspondence, notes, tasks, reassign for admin).
  - Clients: table + detail (same correspondence/notes/tasks; advisor + WhatsApp).
  - Tasks: to do / completed; linked to lead or client; role-filtered.
  - Visibility: admin sees all; staff see only assigned leads/clients/tasks.

Client Portal (client role only):
  - Dashboard: My Advisor (WhatsApp/Call), portfolio value, holdings, Recharts.
  - Documents: list + mock upload (real storage planned via Supabase).
  - Messages: unified thread + mock reply (same thread as CRM correspondence).

Data: lib/mock-crm.ts (leads, clients, staff, correspondence, tasks, notes; service
categories aligned with AS Brokers solutions). lib/mock-portal.ts (holdings,
documents, messages, advisor). lib/mock-auth.ts (session cookies).

Full documentation: CRM_AND_PORTAL.txt (features, benefits, how it works, routes, files).

--------------------------------------------------------------------------------
14. PHASE 4: ANALYTICS & POPIA COOKIE CONSENT
--------------------------------------------------------------------------------

Purpose: Enterprise analytics (GA4, Hotjar) with strict POPIA compliance: no
non-essential tracking until the user explicitly accepts cookies.

Cookie consent (components/ui/CookieConsent.tsx):
  - Fixed bottom banner; glassmorphism (bg-[#0a0a0c]/90 backdrop-blur-xl ring-white/10).
  - Copy: cookies for analytics and saving calculator progress; POPIA compliance.
  - Buttons: "Accept All" (enables GA4 + Hotjar), "Essential Only" (no analytics).
  - Choice persisted in localStorage (key: asbrokers-cookie-consent). Banner only
    shown after client hydration to avoid flash for returning users.

Consent state (components/analytics/ConsentProvider.tsx):
  - React context: consent ("all" | "essential" | null), setConsent, hasChosen.
  - On mount, reads localStorage; ConsentProvider wraps app and renders
    CookieConsent + ConditionalAnalytics.

Conditional script loading:
  - GA4: @next/third-parties/google <GoogleAnalytics gaId="..." />. Mounted only
    when consent === "all" (ConditionalAnalytics).
  - Hotjar: components/analytics/HotjarAnalytics.tsx; next/script strategy="lazyOnload",
    injected only when consent === "all". Snippet loads Hotjar async.
  - If user has not consented or chooses "Essential Only", GA4 and Hotjar never mount.

Root layout:
  - ConsentProvider wraps app content; renders CookieConsent (when hasHydrated) and ConditionalAnalytics.
  - No global MotionConfigProvider (motion respects prefers-reduced-motion per component).

Env:
  - NEXT_PUBLIC_GA_MEASUREMENT_ID (GA4 measurement ID, e.g. G-XXXXXXXX).
  - NEXT_PUBLIC_HOTJAR_ID (Hotjar site ID). See .env.example.

--------------------------------------------------------------------------------
15. CI/CD & QUALITY GATES (PHASES 9–12)
--------------------------------------------------------------------------------

GitHub Actions (.github/workflows/ci.yml) — runs on push/PR to main:

  Job                  | Command / tool                    | Gate
  ---------------------|-----------------------------------|----------------------------------
  build                | npm run build                     | TypeScript + Next.js compile
  accessibility        | npm run test:a11y                 | axe-core WCAG 2.1 AA (Playwright)
  visual-regression    | tests/visual.spec.ts              | Playwright Docker baselines (2% max diff)
  lighthouse           | npm run test:lighthouse           | Mobile perf ≥90, CLS ≤0.1 on / and /contact
  seo-wrs              | npm run test:seo                  | SSR, hydration, payload, Googlebot UA
  master-audit         | npm run test:master               | Aggregates lint, tsc, build, B2A, SEO static, a11y, SEO

Key npm scripts:
  npm run test:a11y         – Playwright + @axe-core/playwright
  npm run test:seo          – WRS / crawler compatibility (15 tests)
  npm run test:visual       – Screenshot regression (homepage + contact)
  npm run test:visual:update – Regenerate baselines (Linux Docker recommended)
  npm run test:lighthouse   – Lighthouse CI (lighthouserc.js)
  npm run test:perf:verify  – Phase 9 manifest + baseline checks
  npm run test:master       – Phase 12 master gate (scripts/verify-phase12-master.ts)

Note: Lighthouse performance gate runs on Linux CI only; local Windows scores are not authoritative.

Playwright config: playwright.config.ts (port 3120, production server via npm run start).

--------------------------------------------------------------------------------
16. PERFORMANCE ARCHITECTURE (PHASE 9)
--------------------------------------------------------------------------------

Homepage optimisations (current build output):
  - Route / is statically generated (○).
  - First Load JS ~163 kB (shared ~103 kB + page ~4 kB).
  - MarketingNavServer: SSR nav without full client Nav bundle on /.
  - HeroSection: server component (no Framer Motion on LCP).
  - BackgroundOrbs: CSS gradients instead of filter: blur().
  - Below-fold: HomeDeferredBlocks wraps Recharts/Framer sections; DeferUntilVisible (rootMargin 0).
  - HomeInsightsTeaserStatic replaces live Sanity fetch on homepage critical path.
  - AppShellChrome dynamically imported (footer, WhatsApp, lead form).
  - ContactEnquiryFormLazy on /contact.
  - SpeculationRulesClient prerenders /contact after idle.
  - System font stack (no next/font on root layout).

Lighthouse CI (lighthouserc.js):
  - URLs: / and /contact; 3 runs each; mobile form factor.
  - Assertions: performance ≥0.9 (error), CLS ≤0.1 (error), LCP ≤2500ms (warn).
  - CPU slowdown: 4× on CI, 2× local.
  - Reports: lhci-reports/ (gitignored).

--------------------------------------------------------------------------------
17. DEPLOYMENT READINESS
--------------------------------------------------------------------------------

Pre-deploy (automated):
  - All CI jobs green on main (especially master-audit).
  - npm run test:master passes on Linux.

Pre-deploy (manual):
  - Production env: NEXT_PUBLIC_SITE_URL=https://www.asbrokers.co.za
  - Supabase RLS smoke test (client/staff/admin visibility)
  - RESEND_API_KEY for contact form email
  - HubSpot token when CRM sync enabled
  - GSC domain verification + sitemap submit

Post-deploy:
  - Request indexing for /, /contact, /calculators, /about, /everest-wealth, /solutions
  - npm run gsc:canonical (service account)
  - Weekly: GSC Coverage, Core Web Vitals, npm run seo:audit --live

Full checklist: docs/DEPLOYMENT.md

Current automated status (local, June 2026):
  - Build, lint, tsc: PASS
  - test:a11y: 5/5 PASS
  - test:seo: 15/15 PASS (zero hydration mismatches)
  - test:visual: 2/2 PASS (local baselines)
  - test:master: PASS (Lighthouse gated on Linux CI)
  - Static SEO audit: 0 findings
  - B2A: ~1,268 tokens (ceiling 128,000)

--------------------------------------------------------------------------------
END OF PROJECT OVERVIEW
================================================================================

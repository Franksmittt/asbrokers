================================================================================
  AS BROKERS CRM / PORTAL BUILD LOG — r5
  Merged Apple × Samsung "Nightography" CRM Polish
  Date: June 2026
================================================================================

TASK 1 — NIGHTOGRAPHY WCAG CONTRAST
-----------------------------------
- Primary body / lead names / metrics → text-gray-100 or text-white
- text-gray-400 reserved for labels, timestamps, disabled, column counts
- H1/H2 → tracking-[-0.03em] or tracking-tight on dashboard & kanban headers
- tabular-nums on all pipeline counts, scores, AUM, conversion %
- CRM layout shell → text-gray-100 antialiased

TASK 2 — ONE UI DASHBOARD (CrmDashboardClient.tsx)
--------------------------------------------------
- Top ~1/3 viewing area (min-h-[32vh]): hero title + massive KPI numerals (text-5xl/6xl)
- Six stage tiles with bold pipeline counts
- Conversion ring embedded in clients KPI card
- Bottom ~2/3 interaction area: tasks, correspondence, recent leads (rim-light Bento)
- All spacing on 4px baseline: gap-4, gap-8, p-4, p-6, p-8

TASK 3 — KANBAN DRAG VS CLICK (LeadsKanban.tsx)
------------------------------------------------
- Removed layout/layoutId (reduces dev sluggishness from FLIP animations)
- Navigation via Framer onTap (does not fire after drag)
- Drag threshold 8px + suppressTapUntilRef 500ms backup
- onDragEnd preventDefault/stopPropagation when dragged
- No nested <Link> on cards

TASK 4 — STATE PERSISTENCE (CrmDemoContext.tsx)
-----------------------------------------------
- leads[] + updateLeadStatus + getLeadById in global provider
- visibleLeads role-filtered from persisted leads
- Context value memoized to reduce re-renders
- LeadsKanban + LeadDetailPageClient consume context (drag survives Back nav)

VERIFICATION
- npx tsc --noEmit

TEST
1. /crm/kanban — drag card, no navigation
2. Tap card — opens lead detail
3. Back to Kanban — card stays in new column
4. /crm — One UI hero metrics + interaction widgets

================================================================================
END r5
================================================================================

export const metadata = {
  title: "Wealth Projection | AS Brokers",
  robots: "noindex, nofollow",
};

export default function PdfReportLayout({ children }: { children: React.ReactNode }) {
  return children;
}

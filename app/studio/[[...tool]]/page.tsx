import { StudioClientWrapper } from "./StudioClient";

export const metadata = {
  title: "Sanity Studio | AS Brokers",
  robots: "noindex",
  referrer: "same-origin",
};

export default function StudioPage() {
  return <StudioClientWrapper />;
}

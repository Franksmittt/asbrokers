================================================================================
  AS BROKERS CRM / PORTAL BUILD LOG — r13
  Phase 7.5: Pre-Flight Verification & Master Audit
  Date: 26 June 2026
================================================================================

CONTEXT
-------
Phase 7 CRM/portal backend wiring (Drizzle + Supabase) was complete after r12.
Before client database push and external API configuration, a full pre-flight
diagnostic was requested using the repository's built-in npm scripts.

Session interrupted 25 June 2026 when internet dropped mid-audit. Resumed and
completed 26 June 2026 in C:\Users\User1\Downloads\asbrokers-main.

SAFETY RULES OBSERVED
---------------------
- READ-ONLY diagnostic mode unless build-blocking errors found
- Relied on package.json scripts + PROJECT_OVERVIEW.txt
- Did NOT touch app/studio/ or app/(content)/ marketing logic beyond a11y fixes
- No database push or external API configuration performed

================================================================================
WHAT COMPLETED LAST NIGHT (25 JUNE) BEFORE INTERRUPTION
================================================================================

PASS:
- npx tsc --noEmit — strict TypeScript clean across app/actions/crm.ts + schema

PARTIAL / FAIL:
- npm run build — started; failed (disk ENOSPC + CRM prerender forbidden() error)
- npm run test:a11y — never ran (disk full)
- npm run seo:audit — ran; 17 findings written to asbrokers_*_findings.txt

ALREADY FIXED BEFORE CUT-OFF:
- app/(portal)/portal/page.tsx — Recharts moved to client lazy loader
  (PortalDashboardChartsLazy.tsx with dynamic(..., { ssr: false }))

================================================================================
TASK 1 — TYPE & BUILD VERIFICATION (26 JUNE)
================================================================================

COMMANDS:
- Remove-Item .next; npx tsc --noEmit
- npm run build

RESULT: PASS (both exit 0)

BUILD BLOCKERS FIXED:

1. next.config.ts
   - experimental.authInterrupts: true
   - Enables forbidden() in CRM/WhatsApp Server Actions (Next.js 15 requirement)

2. app/(crm)/crm/layout.tsx
   - export const dynamic = "force-dynamic"
   - Prevents static prerender of auth-gated CRM pages at build time
   - Error was: /crm/tasks prerender calling forbidden() without session

BUILD WARNINGS (non-blocking):
- @next/next/no-img-element in BlogStudioClient, CrmSidebar, InsightsFeedFilter
- webpack PackFileCacheStrategy big-string serialization notice
- Edge runtime pages skip static generation (expected)

ROUTE SUMMARY (production build):
- 61 static pages generated
- All /crm/* routes marked dynamic (ƒ)
- /portal, /portal/documents, /portal/messages static (○)
- Middleware 82.1 kB

================================================================================
TASK 2 — ACCESSIBILITY GATE (26 JUNE)
================================================================================

COMMAND:
- npm run test:a11y (Playwright + axe-core WCAG 2.1 AA)

RESULT: PASS — 5/5 tests

  ok  home page (/)
  ok  primary conversion page (/contact)
  ok  dynamic insight template
  ok  mobile nav keyboard
  ok  contact form labels

A11Y FIXES APPLIED:

1. components/AppShell.tsx + AppShellChrome.tsx
   - Single #main-content landmark per page (removed duplicate <main> in chrome)
   - Fixed Playwright strict-mode violation on /contact

2. tailwind.config.ts
   - cinematic-teal: #008080 → #00A3A3 (WCAG AA on void/black backgrounds)

3. components/Nav.tsx
   - Planning tools dynamic loading placeholder: text-zinc-500 → text-zinc-400
   - Passes contrast on contact page bg #0a0a0c

NOTE:
- tests/accessibility.spec.ts covers marketing pages only (/ , /contact, insights)
- /crm and /portal routes are NOT yet in the a11y spec — add in a future phase

================================================================================
TASK 3 — SEO LINK GRAPH AUDIT (26 JUNE)
================================================================================

COMMAND:
- npm run seo:audit (static + runtime + internal link graph)

RESULT: 17 findings (exit 1) — non-blocking for DB push

STATIC (8 MEDIUM) — asbrokers_static_seo_aeo_findings.txt
- CRM pages correctly noindex (business-risk-reviews, calculators, etc.)
- Dynamic CRM routes flagged missing generateStaticParams (expected for auth app)
- /crm dashboard missing <h1> in page component (cosmetic SEO; private route)

RUNTIME (7 MEDIUM) — asbrokers_runtime_seo_aeo_findings.txt
- GPTBot / ClaudeBot expected 403 on live www.asbrokers.co.za — got 200
  (production deploy not yet carrying latest middleware; local middleware correct)
- Canonical mismatch on live homepage (page vs /index) — production config issue

LINK GRAPH (2 HIGH) — asbrokers_internal_link_graph_audit_findings.txt
- 7 orphan pages: /about, /chat, /conflict-of-interest, /insights/semigration-
  retirement, /lab, /quiz, /team
- Pre-existing marketing nav structure; not introduced by CRM middleware

MIDDLEWARE CHECK:
- Public routes / and /contact — no false 403 or broken redirects
- /crm and /portal — Supabase SSR auth gate working
- /studio isolation preserved
- Training-bot block (isBlockedTrainingBot) active in middleware.ts

================================================================================
FILES TOUCHED (r13)
================================================================================

Modified:
- next.config.ts                          — authInterrupts
- app/(crm)/crm/layout.tsx                — force-dynamic
- components/AppShell.tsx                 — single main landmark
- components/AppShellChrome.tsx           — removed nested main
- tailwind.config.ts                      — cinematic-teal contrast
- components/Nav.tsx                      — loading placeholder contrast

Log artifacts created:
- build-preflight.log
- a11y-preflight.log
- seo-preflight.log

================================================================================
PRE-FLIGHT VERDICT
================================================================================

| Gate                         | Status                          |
|------------------------------|---------------------------------|
| TypeScript (tsc --noEmit)    | PASS                            |
| Production build             | PASS                            |
| Accessibility (tested pages) | PASS (5/5)                      |
| SEO audit                    | FINDINGS (expected / pre-exist) |

READY FOR DATABASE PUSH: YES
- Codebase compiles and type-checks cleanly
- CRM auth + dynamic rendering stable at build time
- Marketing pages pass axe WCAG 2.1 AA on tested routes

NOT YET DONE:
- git commit / push to GitHub (all CRM + r13 fixes still local)
- npm run db:push (client action — tomorrow)
- Meta WhatsApp + Trigger.dev env on production
- CRM/portal pages in accessibility.spec.ts
- Deploy latest middleware to production (bot blocking + canonical)

================================================================================
DEPLOY CHECKLIST (UNCHANGED FROM r12 + r13 GATES)
================================================================================

- npm run db:push (global_notes, lead_reminders, crm_tasks, crm_leads, etc.)
- Supabase: magic link + app_metadata.role (admin | staff | client)
- Meta WhatsApp env vars for outbound send
- TRIGGER_SECRET_KEY + worker deploy for inbound WhatsApp
- Seed crm_leads with raw_payload { name, phone, email, funnelData, estimatedCapital }
- npm run build on CI/deploy host after push
- Verify middleware bot blocks on production after deploy

================================================================================
VERIFICATION COMMANDS RUN (26 JUNE 2026)
================================================================================

- npx tsc --noEmit                          — PASS
- npm run build                             — PASS
- npm run test:a11y                         — PASS (5/5)
- npm run seo:audit                         — 17 findings (documented above)

================================================================================
END r13
================================================================================
